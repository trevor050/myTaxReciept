import { config } from 'dotenv';
config();

// Removed import '@/ai/flows/suggest-representatives.ts';
// Genkit related initializations or flow definitions would go here if any were used.
// For now, this file can be kept minimal or used for other development-specific AI setup if needed in the future.
